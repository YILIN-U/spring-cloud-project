server:
  port: 7001
eureka:
  instance:
    instance-id: eureka_center
    prefer-ip-address: true
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:7001/eureka
spring:
  application:
    name: server_center
